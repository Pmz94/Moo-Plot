@model IEnumerable<MooPlot.Models.animales_enfermos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.enfermedade.Nom_Enfermedad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.animale.Especie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.enfermedade.Nom_Enfermedad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.animale.Especie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_enfermo }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_enfermo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_enfermo })
        </td>
    </tr>
}

</table>
